<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.treniroval">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ItemLayout.item_layout_past_trainings"></activity>
        <activity android:name=".activitys.PastTrainingsActivity" />
        <activity android:name=".activitys.TrainingConstructorActivity" />
        <activity android:name=".activitys.TrainingComplitedActivity" />
        <activity android:name=".activitys.SelectTrainingTopicActivity" />
        <activity android:name=".activitys.CurrentPastTainingActivity" />
            <activity android:name=".activitys.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>